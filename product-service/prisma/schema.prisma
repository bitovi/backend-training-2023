generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DB_URL")
}

model product {
  id    Int     @id
  name  String?
  price String?
  availableQuantity Int?
}
