generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DB_URL")
}

model product {
  productid String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name      String
  amount    Int
  type      String
}
