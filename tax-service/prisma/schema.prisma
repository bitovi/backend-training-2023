generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DB_URL")
}

model tax {
  taxid        String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  product_type String @unique(map: "product_type_unique")
  percentage   Float
}
